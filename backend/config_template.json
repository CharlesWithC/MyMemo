{
    "server_ip": "127.0.0.1",
    "server_port": 8888,
    "max_concurrent_download": -1,

    "default_user_password": "123456",

    "database": "mysql", // alternative: sqlite
    // if you use sqlite as database, the script will automatically create a database.db
    // you don't need to do anything else
    // if you use mysql as database, fill the following data
    // you should create a dedicate user for mymemo, and create a database called mymemo to store data
    // the following could be empty and they will be asked when you run the script
    "mysql_host": "localhost",
    "mysql_user": "mymemo",
    "mysql_passwd": "123456",
    "mysql_db": "mymemo",

    "postfix_host": sendVerification,

    "max_user_allowed": -1,
    "max_question_per_user_allowed": -1,
    "max_book_per_user_allowed": -1,

    "allow_group_creation_for_all_user": false,
    "max_group_member": 50,

    "allow_register": true,
    "use_invite_system": false
}